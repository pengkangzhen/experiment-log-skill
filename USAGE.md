# 实验日志工具 - 使用指南

本指南详细介绍实验日志工具的使用方法、工作流和最佳实践。

---

## 目录

1. [完整命令手册](#完整命令手册)
2. [自然语言使用技巧](#自然语言使用技巧)
3. [典型工作流](#典型工作流)
4. [导出报告格式](#导出报告格式)
5. [最佳实践](#最佳实践)

---

## 完整命令手册

### `/explore [问题描述]`

启动新实验或查看当前实验状态。

**用法：**
- `/explore` - 显示当前实验信息
- `/explore 解决数据库慢查询问题` - 创建新实验

**自然语言替代：**
- "启动实验，解决数据库慢查询问题"
- "开始记录实验：优化首页性能"
- "创建新实验，探索新架构方案"

---

### `/try <描述> [--branch]`

在当前节点下添加一个新的尝试节点。

**用法：**
- `/try 添加数据库索引` - 添加子节点
- `/try 使用缓存方案 --branch` - 创建分支（兄弟节点）

**自然语言替代：**
- "尝试添加数据库索引方案"
- "记录尝试：使用 Redis 缓存"
- "开始尝试重构核心算法"

**提示：** 使用 `--branch` 标记当你想尝试一个平行的替代方案，而非当前方案的延伸。

---

### `/result <success|failed|aborted> [描述]`

记录当前节点的尝试结果。

**状态说明：**
- `success` - 尝试成功，方案可行
- `failed` - 尝试失败，方案不可行
- `aborted` - 放弃此方案（如过于复杂、成本过高等）

**用法：**
- `/result success 查询时间从 2s 降到 100ms`
- `/result failed 出现内存泄漏问题`
- `/result aborted 方案过于复杂，维护成本高`

**自然语言替代：**
- "这次成功了，响应时间降到 50ms"
- "结果是失败，出现循环依赖"
- "该方案不可行，需要太多改动"
- "完成尝试，测试通过"

---

### `/back`

回退到当前节点的父节点。用于在分支探索失败后返回上级尝试其他方案。

**用法：**
- `/back`

**场景示例：**
```
/explore 解决性能问题
/try 方案 A
/result failed 不起作用
/back           # 返回根节点
/try 方案 B     # 尝试另一个方案
```

---

### `/tree`

以树状图形式展示整个实验探索过程。

**输出示例：**
```
解决 API 慢问题 [a1b2c3d4] (pending)
├── 尝试 Redis 缓存 [e5f6g7h8] (success)
│   └── 优化序列化 [i9j0k1l2] (success)
└── 尝试数据库索引 [m3n4o5p6] (failed)
    └── 添加复合索引 [q7r8s9t0] (aborted)
```

**自然语言替代：**
- "查看实验树"
- "显示探索记录"
- "查看实验结构"
- "实验状态如何"

---

### `/status`

显示当前实验的详细统计信息。

**输出包含：**
- 实验 ID 和问题描述
- 创建和更新时间
- 当前所在节点
- 节点统计（总数、成功、失败、待处理）

---

### `/log-list`

列出所有进行中的和已完成的实验。

---

### `/log-export [实验ID]`

将实验导出为 Markdown 格式的报告。

**用法：**
- `/log-export` - 导出当前实验
- `/log-export abc123` - 导出指定 ID 的实验

**自然语言替代：**
- "导出实验报告"
- "生成实验日志"
- "导出日志"

导出的文件名为 `experiment_{ID}_{时间戳}.md`。

---

### `/log-close`

结束当前实验，标记为已完成。

**自然语言替代：**
- "关闭实验"
- "停止记录"
- "结束实验"
- "停止实验"

---

## 自然语言使用技巧

### 基本原则

自然语言识别支持灵活的表达方式，关键是包含**意图关键词**和**必要信息**。

### 启动实验

关键词：启动、开始、开启、创建、新建

有效示例：
- "启动实验，解决内存泄漏问题"
- "开始记录实验：优化构建速度"
- "创建新实验，探索微服务拆分方案"
- "开始探索 React 18 升级方案"

### 记录尝试

关键词：尝试、测试、记录、采用、使用

有效示例：
- "尝试使用 Webpack 5 的持久缓存"
- "记录尝试：重构路由配置"
- "我要尝试升级 TypeScript 版本"
- "采用函数组件替代类组件"

### 记录结果

关键词：成功、失败、完成、作废、可行、有效

有效示例：
- "这次成功了，构建时间减少 60%"
- "结果是失败，与现有代码冲突"
- "该方案不可行，需要改动太多文件"
- "作废这个方案，维护成本太高"

### 导出和查看

有效示例：
- "导出实验报告"
- "查看实验树"
- "显示当前探索进度"
- "生成实验日志文档"

---

## 典型工作流

### 工作流 1：问题排查流程

适用于定位和解决具体问题（如 Bug、性能问题）。

```bash
# 1. 启动实验，明确问题
/explore 解决登录接口偶尔超时的问题

# 2. 提出假设并尝试
/try 增加数据库连接池大小
# ... 修改代码，测试 ...
/result failed 问题仍然存在

# 3. 基于失败结果调整方向
/try 添加接口超时日志
/result success 发现是第三方 API 响应慢

# 4. 深入解决根因
/try 给第三方 API 调用添加缓存
/result success 超时问题消失

# 5. 查看完整探索过程
/tree

# 6. 导出报告供团队参考
/log-export
/log-close
```

---

### 工作流 2：方案对比流程

适用于比较多个技术方案，选择最优解。

```bash
# 1. 启动实验
/explore 选择前端状态管理方案

# 2. 尝试方案 A
/try 使用 Redux Toolkit
# ... 调研和 POC ...
/result aborted 样板代码过多，不适合小项目

# 3. 尝试方案 B
/try 使用 Zustand
/result success 简洁易用，满足需求

# 4. 尝试方案 C（分支对比）
/back
/try 使用 Jotai --branch
/result success 原子化设计很好，但学习成本略高

# 5. 对比后做出决策
# 根据记录对比各方案优缺点，选择 Zustand

/tree
/log-export
```

---

### 工作流 3：迭代优化流程

适用于逐步优化和改进现有功能。

```bash
# 1. 启动优化实验
/explore 优化首页加载性能

# 2. 第一轮优化
/try 压缩图片资源
/result success 体积减少 40%，但仍有优化空间

# 3. 基于上轮结果继续优化
/try 启用路由懒加载
/result success 首屏 JS 减少 60%

# 4. 继续迭代
/try 添加 Service Worker 缓存
/result success 二次访问秒开

# 5. 查看优化路径
/tree
# 输出将展示清晰的优化演进路径

/log-export
/log-close
```

---

## 导出报告格式

导出的 Markdown 报告包含以下结构：

```markdown
# 实验日志：解决 API 响应慢的问题

- **实验 ID**: a1b2c3d4
- **创建时间**: 2024-01-15 10:30:00
- **最后更新**: 2024-01-15 14:20:00
- **状态**: 进行中

## 实验树

[树状结构图]

## 详细记录

### 节点 1: 解决 API 响应慢的问题
**状态**: 进行中
**时间**: 2024-01-15 10:30:00

### 节点 2: 添加 Redis 缓存
**状态**: 成功
**时间**: 2024-01-15 10:35:00
**描述**: 响应时间从 2s 降到 50ms
...
```

---

## 最佳实践

### 1. 实验粒度控制

- **不要太宽泛**："优化项目" → **不好**
- **不要太细碎**："修改第 3 行的缩进" → **不好**
- **适当具体**："解决用户列表页加载慢的问题" → **好**

### 2. 描述要清晰

好的尝试描述：
- "使用数据库索引优化查询" ✓
- "Redis 缓存 + 5 分钟过期策略" ✓

差的尝试描述：
- "试一下这个" ✗
- "改了一下" ✗

### 3. 及时记录结果

- 在尝试有明确结论后立即记录结果
- 不要等到已经做了多个修改后才记录
- 如果尝试过程中发现新方向，可以 `/back` 后创建分支

### 4. 善用分支功能

当当前路径遇到阻碍时，使用 `/back` 回到分支点，然后 `--branch` 尝试平行方案：

```bash
/try 方案 A
/result failed
/back
/try 方案 B --branch
```

### 5. 定期导出报告

- 实验结束时导出报告，便于复盘
- 重要里程碑也可以导出，保留阶段性成果
- 报告可用于团队分享、文档沉淀

### 6. 结合 Git 使用

实验日志与 Git 提交形成互补：
- Git 记录**代码变化**
- 实验日志记录**思考过程**

推荐在重要尝试后提交代码，并在提交信息中关联实验节点。
